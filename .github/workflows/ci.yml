name: restaurant delivery backend development

on:
  push:
    branches:
      - master
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      RD_DATABASE_NAME: ${{ secrets.RD_DATABASE_NAME}}
      RD_DATABASE_USERNAME: ${{ secrets.RD_DATABASE_USERNAME }}
      RD_DATABASE_PASSWORD: ${{ secrets.RD_DATABASE_PASSWORD }}
      RD_DATABASE_HOST: ${{ secrets.RD_DATABASE_HOST }}

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/xintarius/restaurantdelivery_backend:develop

  deploy:
    runs-on: ubuntu-latest
    environment: development
    steps:
      - name: Execute remote SSH commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_USER }}
          key: ${{ secrets.ORACLE_SSH_KEY }}
          script: |
            cd RestaurantDelivery_Backend
            git fetch origin develop
            git reset --hard origin/develop
            docker-compose down --remove-orphans
            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker compose pull -q
            docker compose up -d
            docker image prune -f